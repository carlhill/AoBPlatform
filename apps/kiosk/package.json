{
  "name": "@aobplatform/kiosk",
  "version": "0.1.0",
  "description": "In-practice tablet/kiosk app (C2) — Expo/React Native, offline-first encrypted local queue, kiosk lockdown, vector+raster signature capture. NOT a patient mobile app (there is none, deliberately). Expo scaffold to be generated with create-expo-app during Phase 1 — placeholder workspace until then.",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
